<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/ignore_layout_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="number"
        android:textSize="20dp"
        android:textColor="#000"
        android:padding="2dp"
        android:focusable="false"
        android:clickable="false"/>

    <TextView
        android:id="@+id/ignore_layout_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="name"
        android:textSize="20dp"
        android:textColor="#000"
        android:padding="2dp"
        android:focusable="false"
        android:clickable="false"
        android:layout_below="@+id/ignore_layout_number"/>

    <Button
        android:id="@+id/remove_ignore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Remove"
        android:textColor="#fff"
        android:backgroundTint="#ef00"
        android:layout_alignParentRight="true"
        android:focusable="false"/>

    <!--<Button
        android:id="@+id/ignore_reminder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ignore"
        android:textColor="#fff"
        android:backgroundTint="#ef00"
        android:layout_toLeftOf="@+id/remove_reminder"/> -->

</RelativeLayout>